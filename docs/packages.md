# Packages

Librerías compartidas de Axis.

- `contracts/` — OpenAPI, tipos compartidos y clientes generados.
- `auth/` — helpers de identidad interna, scopes y token exchange.
- `ui/` — componentes visuales compartidos para `../console`.
