root = "."
tmp_dir = "tmp"

[build]
  cmd = "cd companion && CGO_ENABLED=0 go build -o ../tmp/companion-v2 ./cmd/api"
  bin = "./tmp/companion-v2"
  full_bin = "./tmp/companion-v2"
  include_dir = ["companion"]
  include_ext = ["go", "sql", "yaml", "yml"]
  exclude_dir = ["tmp", "docs", "companion/tmp"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
