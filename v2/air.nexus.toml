root = "."
tmp_dir = "tmp"

[build]
  cmd = "cd nexus && CGO_ENABLED=0 go build -o ../tmp/nexus-v2 ./cmd/api"
  bin = "./tmp/nexus-v2"
  full_bin = "./tmp/nexus-v2"
  include_dir = ["nexus"]
  include_ext = ["go", "sql", "yaml", "yml"]
  exclude_dir = ["tmp", "docs", "nexus/tmp"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
