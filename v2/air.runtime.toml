root = "."
tmp_dir = "tmp"

[build]
  cmd = "cd runtime && CGO_ENABLED=0 go build -o ../tmp/runtime-v2 ./cmd/api"
  bin = "./tmp/runtime-v2"
  full_bin = "./tmp/runtime-v2"
  include_dir = ["runtime"]
  include_ext = ["go", "sql", "yaml", "yml"]
  exclude_dir = ["tmp", "docs", "runtime/tmp"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
