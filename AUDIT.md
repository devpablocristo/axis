# AUDIT.md — Auditoría de calidad del proyecto Axis

## Segunda pasada — 2026-06-27

Alcance: revalidacion post-saneamiento y post-cutover B#3. No se confia ciegamente en hallazgos viejos: se releyeron BFF tenancy/authz, IAM simple, console bootstrap, boundaries Companion/Nexus y adopcion de `platform`.

### Estado actual

- B#3 verificado: `resolveAppContext` exige `X-Tenant-ID`; deriva `org_id`, `product_surface`, `tenant_id` y scopes desde `axis_tenants` + `axis_tenant_members` o platform role.
- B#3 verificado: `userRefOrg`, `scopesFromAxisOrg`, `listIAMUserViews` y `memberUserView` ya no aparecen en `bff`.
- B#3 verificado: `/api/iam/users` para org normal requiere tenant real y lista/muta `axis_tenant_members`; `axis_org_members` queda como bridge Clerk/org picker.
- Guardrail: `make hygiene` OK.
- Guardrail inicial: `make lint` fallo por `console/src/App.tsx:93` (`no-useless-assignment`). Estado: **corregido en PR #54** cambiando `nextSession` a asignacion definida antes de uso.
- Guardrail final: `make lint` OK (mantiene 16 warnings calibrados de console).
- Console: `npm run typecheck && npm run test && npm run build` OK.

### Hallazgos vigentes

#### HIGH-2P-01 — Guardrail de lint roto en console

Estado: **corregido en PR #54**.

Evidencia: `make lint` fallaba en `console/src/App.tsx:93` por `no-useless-assignment`.

Riesgo: main no podia pasar el guardrail de calidad aunque el bug fuera chico.

Fix: `let nextSession: AxisSession` en lugar de inicializar a `null` y reasignar antes de leer.

#### MED-2P-01 — `tenantUserView` conflata error de store con not-found

Estado: **corregido en PR #54**.

Evidencia: `bff/iam_simple_handlers.go` hace `ListTenantMembers`; si falla devuelve `(empty,false)` y los lifecycle handlers pueden renderizar la mutacion como si el user no existiera.

Riesgo: en outage parcial de DB, errores 5xx se convierten en estados 404/no-op y dificultan soporte.

Fix: `tenantUserView` ahora devuelve `(IAMUserView, error)`; update/lifecycle propagan fallas de relectura como 5xx. Tests de regresion cubren update y archive cuando `ListTenantMembers` falla.

#### MED-2P-02 — `controlProvisionTenant` sigue sin atomicidad tenant+owner

Estado: **corregido en PR #54**.

Evidencia: `bff/control_handlers.go` crea tenant con `CreateTenant` y luego, opcionalmente, `UpsertTenantMember` para owner.

Riesgo: si el segundo write falla queda tenant sin owner inicial.

Fix: agregado `CreateTenantWithOwner` a `IAMStore`; `sqlIAMStore` lo ejecuta en una transaccion y `controlProvisionTenant`/`ensureOrgDefaultTenant` usan esa primitiva. Test de contrato evita volver al par de writes sueltos.

#### MED-2P-03 — Algunos proxies BFF devuelven `err.Error()` al cliente

Estado: **corregido en PR #54**.

Evidencia: `bff/prompts_handlers.go`, `bff/agent_profiles_handlers.go` y `bff/agent_handlers.go` aun escriben errores crudos en respuestas para fallas internas/downstream.

Riesgo: exposicion de detalles de infraestructura o contratos downstream; inconsistencia con los helpers `WriteFlatErrorFrom` ya adoptados en Nexus.

Fix: agregado `writeLoggedError`; forwarders de prompts, agent-profiles, agents y proxy generico devuelven mensajes estables y loguean la causa server-side. Tests cubren que transport errors no filtren host/`connect` en el body.

#### LOW-2P-01 — Deadcode pendiente es mayormente soporte test/dev

Estado: **verificado, sin borrado en esta pasada**.

Evidencia: `deadcode` marca `productevals`, `MemoryRepository`, `SignAttestationForTest`, publishers directos y helpers de runtime que estan usados por tests, comandos o superficies de soporte.

Verificacion: `deadcode` actual marca BFF limpio; en Companion/Nexus los principales hits tienen referencias reales en tests/tooling (`productevals` en onboarding/contracts/evals, `MemoryRepository` en tests/watchers, `SignAttestationForTest` en attestation verifier test). Los candidatos baratos del audit viejo (`deleteIAMMember`, `fakePymes`, `preferred`) ya no aplican o ya estan usados.

Decision: no borrar APIs internas exportadas solo porque `deadcode` no sigue test/tooling entrypoints. Mantener como deuda documental; si se quiere reducir ruido, mover helpers test-only a `_test.go` en PR dedicado y con cobertura local.

### Relacion con Platform

Axis ya consume `platform/http/go/httpjson` para normalizar errores en varios dominios Nexus. En esta segunda pasada se corrigieron localmente la atomicidad IAM BFF y el error mapping de proxies BFF; no ameritan extraccion a `platform` todavia. No conviene mover tenancy BFF a kernels SaaS hasta estabilizar nombres `org` vs `tenant` en los consumidores.

### Follow-up — 2026-06-28

- **BFF platform-role lookup fail-closed** (PR #55/#56): `resolveAppContext` y `controlAPI` ya no tratan un error de `PlatformRolesForUser` como ausencia de rol. El proxy app-plane clasifica ese fallo como 500 estable/logueado y Control Plane tambien responde 500 estable, evitando falsos 403 y filtrado de detalles en outage de store.
- **BFF auth platform-role lookup fail-closed** (PR #57): el upgrade de scopes en `authenticate` ya no ignora errores de `PlatformRolesForUser`; responde 500 estable/logueado y evita degradar permisos silenciosamente cuando falla el store.
- **BFF org-access lookup fail-closed** (PR #58): `selectedOrg`, `canAccessOrg` y agent/IAM callers ya no convierten errores de `ActorCanAccessOrg`/`ListOrgsForActor` en 403/not-found; responden 500 estable/logueado sin filtrar el error del store.
- **BFF manual app-context handlers fail-closed** (PR #59): prompts y agent-profiles usan el mismo `writeAppContextError` que el proxy genérico, evitando falsos 403 y filtrado de errores internos al resolver tenant/scopes.
- **Nexus submit cross-org usa el org solicitado preservado** (PR #60): `bindParamsToPrincipalOrg` ya no compara `params.org_id` contra el `X-Org-ID` rebindeado al org del API key cuando el principal tiene `nexus:cross_org`; usa `orgctx.Narrowed`, igual que list/read. Esto desbloquea productos externos como Medmory registrando requests sensibles para su org real (`cristo.tech`) con una service/admin key bound a `axis`. Verificado con `cd nexus && go test ./internal/requests`.

### Follow-up — 2026-06-29

- **BFF `stg`/`preview` dejan de confiar en headers dev** (PR #62): `authenticate` ahora solo acepta `X-Dev-*` cuando `AXIS_BFF_AUTH_MODE=dev`; `stg`, `preview`, `oidc` y `clerk` requieren Bearer/OIDC y `newServer` exige `AXIS_AUTH_ISSUER_URL`. Los workflows de STG/preview/PRD validan issuer/audience para cualquier modo no-dev. Tests: `TestStgAndPreviewAuthModesDoNotTrustDevHeaders`, `TestDevAuthModeStillTrustsDevHeaders`, `TestNewServerRequiresIssuerForDeployedAuthModes`.

Fecha: 2026-06-27 · Método: workflow multi-agente (138 agentes, 14 revisores × dimensión/módulo + verificación adversarial). **106 hallazgos confirmados** (25 HIGH / 43 MED / 38 LOW), 19 descartados (by-design/falso-positivo).

> Cada hallazgo fue **verificado leyendo el código**; los descartados (apéndice) eran by-design o falsos positivos (ej. dead-code que es test-only).

## Estado de ejecución (actualizado 2026-06-27)

**Hecho + commiteado** (cada oleada: build+vet+test+staticcheck 0):
- **Oleada A (HIGH)** — error-handling (webhooks Clerk dejan de tragar; helpers `writeWatcherError`/`writeUsecaseError`/`writeOpsError` mapean `domainerr`→status en bff/companion/nexus), authz gaps (`canAccessOrg` en update/lifecycle), atomicidad (`SetTenantMembership` en tx). Commits `88ae7a4`, `c33f59d`, `1a16981`.
- **Oleada C (dead-code/lint)** — 3 módulos Go a staticcheck **0**; dead-code confirmado removido (test-only preservado); guardrails instalados.
- **Oleada B#1 (nexus product-aware)** — migración `0024` (`product_surface` + índice `idx_requests_org_product`, aplicada y verificada en DB), `productctx` + extracción del claim de JWT, y **threading write+round-trip en el dominio `requests`** (entity/SubmitInput/INSERT/SELECT/scan/DTO). Aditivo y retrocompatible. Commit `27c60f7`.
- **Oleada B#2 (authz off-Clerk)** — los permisos los deriva **Axis** (platform roles + membership), no el token de Clerk, con fallback logueado para no lockear durante la migración; sync de borrado (`user.deleted`/`organization.deleted`) en el webhook. Commit `5b89de7`.
- **Fase 3** — `make lint` = 0 (staticcheck limpio ×3; reglas react-hooks v7 de perf calibradas a `warn`, ver §0). Coverage baseline registrado. Commit `91aceaf`.

**Gated (necesitan decisión/dependencia externa, NO aplicados a ciegas):**
- **B#1 read-filter + otros dominios** (approvals/policies/ops/learning/dashboard): filtrar `List` por producto **ahora** ocultaría filas existentes (`product_surface=''`) a callers con producto → cambio de comportamiento. Requiere emisión de JWT de producto completa + decisión de backfill.
- **B#3 borrar paths legacy por-org** (`userRefOrg`, dual-path): **el console SÍ depende** — `console/src/api.ts:391-393` manda `X-Tenant-ID` solo si hay `axis.tenant_id` en localStorage; sin tenant seleccionado cae al path legacy (`listIAMUserViews`). Bloqueante: el console debe garantizar `X-Tenant-ID` siempre antes de remover el fallback.
- **Oleada D (reuse → `platform`)**: consolidar los 3 helpers de error en un `WriteFlatErrorFrom` agnóstico + adoptar `security/go/tenant`/`lifecycle`. Cross-repo (toca `platform` con blast radius) → necesita release/bump de versión.

## 0. Guardrails (instalados + corridos)

- **staticcheck:** bff 1 · companion 17 · nexus 0 — triados abajo.
- **deadcode:** bruto bff 1 / companion 32 / nexus 4; tras filtrar test-only → ver §Dead code.
- **eslint (console, nuevo `eslint.config.js`):** 26 problemas (incl. `set-state-in-effect`, `refs-in-render`).
- **coverage baseline:** bff 50.9%.
- Targets nuevos: `make lint` / `lint-go` / `lint-console` / `coverage`.

## 1. Bugs y deuda (por severidad)

### 🔴 HIGH (25)

- **Authz still derived from Clerk claims in PrincipalFromClaims/axisPrincipalFromIdentity** — [bff] `bff/identity_clerk.go:63` · `debt` · effort `l`
  - *Qué:* On every authenticated request, clerkIdentityAdapter.PrincipalFromClaims calls axisPrincipalFromIdentity (identity.go:116), which sets p.Role and p.Scopes purely from the Clerk JWT claims axis_role/org_role via scopesForIdentityRoles (identity.go:128-143). This is the OLD org-based model: a Clerk owner gets full defaultAdminScopes, a Clerk org admin/member gets orgAdminScopes/orgMemberScopes — all with zero reference to axis_tenant_members or axis_platform_roles. The new model (scopesForTenant, identity.go:164) is only consulted inside resolveAppContext for proxied app-plane calls that carry X
  - *Fix:* Make Clerk claims identity-only: have PrincipalFromClaims set Actor/OrgID/AuthMethod but NOT p.Scopes. Derive p.Scopes for the control plane from axis_platform_roles (isPlatformAdmin) + the actor's tenant/org memberships in Axis, mirroring scopesForTenant. Delete scopesForIdentityRoles + axisPrincipalFromIdentity once the IAM handlers read Axis-derived scopes.
- **Legacy /api/iam/tenants operates on axis_orgs, not axis_tenants — duplicate 'tenant' surface vs /api/control/tenants** — [bff] `bff/iam_simple_handlers.go:63` · `debt` · effort `l`
  - *Qué:* iamTenants() presents IAMTenantView and audits 'tenant.created/updated/purged', but every operation routes to createIAMOrg/updateIAMOrg/deleteIAMOrg and ListOrgsForActor (lines 70, 94, 111, 135, 154) — i.e. it CRUDs axis_orgs (Clerk organizations) while calling them 'tenants'. The real tenancy model (axis_tenants = org x product) is served by a SECOND, parallel surface: controlAPI /api/control/tenants (control_handlers.go:70-113) using ListTenants/CreateTenant. Two endpoints both named 'tenants' mean opposite things; the console must know which is which, and a 'tenant' purged via /api/iam actu
  - *Fix:* Rename the legacy surface to what it is (organizations) and fold its CRUD into the org concept, or migrate the console fully onto /api/control/tenants for org x product and drop IAMTenantView's 'tenant' framing. Pick one tenant surface.
- **stg and preview auth modes silently use the dev (header-trust) authentication path** — [bff] `bff/main.go:194` · `bug` · effort `m`
  - *Qué:* authenticate() treats AuthMode in {"dev","preview","stg"} identically: it fabricates a principal from X-Dev-Org-ID / X-Dev-User-ID / X-Dev-Role / X-Dev-Scopes headers and never validates a bearer token, never calls the OIDC authenticator, and never invokes s.identity.PrincipalFromClaims / SyncPrincipal. Per .github/workflows/deploy-stg.yml:65 (AXIS_BFF_AUTH_MODE default 'stg') and preview-deploy.yml:53 (default 'preview'), these are the modes actually deployed to the shared STG project and to every PR preview. That means a deployed STG/preview Axis BFF will mint an internal JWT with full admin
  - *Fix:* Decide explicitly whether stg/preview are meant to be authenticated. If they should use Clerk, route stg/preview through the same oidcAuth + s.identity branch as oidc/clerk (only 'dev' should trust X-Dev-* headers). If they are intentionally header-trust, gate that path behind a single isDevMode() helper and add a test asserting stg/preview do NOT accept unauthenticated requests in deployed config
- **Scopes are derived three different, mutually-divergent ways across auth modes; only the dev derivation is tested end-to-end** — [bff] `bff/main.go:199` · `bug` · effort `m`
  - *Qué:* Principal scopes come from a different source per mode: (1) dev/stg/preview -> verbatim X-Dev-Scopes or cfg.DevScopes (main.go:199); (2) oidc without identity -> verbatim claimScopes(claims) from the token's scope/scp claim (main.go:150); (3) clerk -> ignores token scopes entirely and RECOMPUTES them from axis_role/org_role via axisPrincipalFromIdentity->scopesForIdentityRoles (identity.go:124,128). All HTTP-level authz tests (main_test.go, iam_users_test.go) run in dev mode and feed scopes directly, so they validate scope-gating but NEVER validate that clerk's role->scope mapping (scopesForId
  - *Fix:* Add table-driven tests that build a principal via clerkIdentityAdapter.PrincipalFromClaims for owner/admin/member claims and assert the resulting Scopes match orgAdminScopes()/orgMemberScopes()/defaultAdminScopes(). Better, unify scope derivation: have the handlers depend on a single ScopesForPrincipal(store, principal) used by all three modes so dev tests actually cover the production code path.
- **Control Plane (/api/control/) is unreachable for the dev admin and has zero tests, so the clerk-only platform-admin bridge is unverified** — [bff] `bff/control_handlers.go:18` · `bug` · effort `m`
  - *Qué:* controlAPI gates every route on isPlatformAdmin(PlatformRolesForUser(actor)). In dev/stg/preview mode the principal carries no claims and SyncPrincipal is never called, so no axis_platform_roles row is ever seeded for the dev user -> the dev admin gets 403 on all of /api/control/ (organizations, tenants, products, platform-roles, tenant members). The ONLY thing that grants platform_admin is clerk SyncPrincipal bridging axis_role=owner -> SetPlatformRole(actor,'platform_admin') (identity_clerk.go:86-90), which runs solely in clerk mode. There is no test for controlAPI at all (grep shows no /api
  - *Fix:* Add controlAPI tests in dev mode that explicitly seed a platform role (via iam.SetPlatformRole) and assert 200, plus a negative test asserting 403 without it. Separately, provide a dev path to obtain platform_admin (e.g. honor X-Dev-Platform-Roles or seed from cfg) so the Control Plane is testable/usable locally, mirroring the clerk bridge.
- **createIAMUserView tenant path runs 3-4 writes (Clerk + 3 tables) with no transaction or compensation** — [bff] `bff/iam_simple_handlers.go:459` · `bug` · effort `l`
  - *Qué:* The tenant-aware create at lines 459-484 performs a sequence of independent writes: (1) find-or-create the identity — for a NEW user createIAMUser does Clerk POST /users + axis_users INSERT + axis_org_members UPSERT (identity.go:56-62 / identity_clerk.go:261-269); for an existing user upsertIAMMember writes axis_org_members; (2) if role==owner, s.iam.SetPlatformRole writes axis_platform_roles (line 476); (3) s.iam.UpsertTenantMember writes axis_tenant_members (line 481). These are 3-4 distinct round trips with no enclosing tx (IAMStore exposes no Begin/Tx primitive; sqlIAMStore uses s.db.Query
  - *Fix:* Wrap the axis_* writes (CreateUser-local, UpsertMember, SetPlatformRole, UpsertTenantMember) in a single DB transaction so they commit or roll back together. Concretely: add a transactional method to IAMStore (e.g. ProvisionTenantMember(ctx, user, orgMember, platformRole, tenantMember) executed inside one sql.Tx via s.db.BeginTx), and call it from createIAMUserView. The Clerk POST /users is extern
- **updateIAMUserView tenant path: UpdateUser + platform-role flip + tenant-member upsert are non-atomic** — [bff] `bff/iam_simple_handlers.go:526` · `bug` · effort `m`
  - *Qué:* In the userRefTenant branch (lines 526-550): optional email update via updateIAMUser (Clerk PATCH /users + axis_users UPDATE), then either SetPlatformRole(owner) or RemovePlatformRole(owner) on axis_platform_roles (lines 537-547), then UpsertTenantMember on axis_tenant_members (line 548). No transaction. The role demotion case is the dangerous one: RemovePlatformRole('owner') succeeds (user loses global super-admin) but if the following UpsertTenantMember fails, the membership/role change is lost while the platform-role removal persists — the resulting state matches no requested input. Convers
  - *Fix:* Group RemovePlatformRole/SetPlatformRole and UpsertTenantMember (and the local axis_users update when email changes) into one sql.Tx so the role transition and the membership row commit together. Keep the Clerk PATCH outside the tx (ordered first) and reconcile via webhook; the authz-bearing axis_* rows must be atomic. Same transactional store method/helper as the create path can serve both.
- **Clerk webhook fallback swallows CreateUser/CreateOrg errors and still returns 202** — [bff] `bff/iam_handlers.go:86` · `bug` · effort `s`
  - *Qué:* In clerkWebhook, the no-identity-adapter fallback path discards the store result entirely: `_, _ = s.iam.CreateUser(...)` (line 86) and `_, _ = s.iam.CreateOrg(...)` (line 96). After the switch the handler unconditionally responds `writeJSON(w, http.StatusAccepted, {"status":"accepted"})` (line 106). If the DB insert fails (constraint violation, connection drop), Clerk receives 202 and never retries, so the user/org is permanently dropped from axis_users/axis_orgs. This is the exact path flagged in the audit prompt. The identity-adapter branch above (lines 73-78) does the right thing via write
  - *Fix:* Capture the error from CreateUser/CreateOrg and route it through writeStoreError (returning 5xx so Clerk retries) instead of discarding it. Better: collapse the two branches so the fallback path reuses the same error handling as the identity-adapter path.
- **writeStoreError collapses every non-sentinel error to a generic 500, hiding upstream 4xx and validation causes** — [bff] `bff/iam_handlers.go:234` · `bug` · effort `m`
  - *Qué:* writeStoreError only special-cases errNotFound and errIdentityProviderNotConfigured; everything else falls through to `http.StatusInternalServerError, INTERNAL_ERROR, "request failed"` (line 243). This swallows the cause of: (a) clerkAPIError, which already carries a real upstream StatusCode (identity_clerk.go:21-37, with a clerkStatus() accessor) - a Clerk 400/409/422 (e.g. duplicate email, invalid role) is reported to the console as a 500; (b) plain validation errors like `fmt.Errorf("email is required")` (identity_clerk.go:241) surfaced as 500. The IAM create/update/lifecycle handlers in ia
  - *Fix:* Map clerkAPIError via clerkStatus(err): a 4xx upstream should become the corresponding 4xx (409/422/400) with the upstream message, not 500. Add a sentinel/typed validation error for input problems so they map to 400. Keep the generic 500 only for genuinely unknown errors, and log the underlying err.
- **Tenant update and lifecycle (archive/trash/restore/purge) skip canAccessOrg - only a scope is checked** — [bff] `bff/iam_simple_handlers.go:101` · `bug` · effort `s`
  - *Qué:* In iamTenants, PUT/PATCH (lines 103-117) and iamTenantLifecycle (lines 126-159) gate only on requireScope(axis:orgs:write/admin) or requireScope(axis:iam:purge) and then call s.updateIAMOrg(tenantID,...) / s.deleteIAMOrg(tenantID) directly. There is no s.canAccessOrg(r, p, tenantID) check binding the actor to that specific tenant/org. Contrast this with the sibling resources that DO bind access to the target: products update/lifecycle go through productForAccess() which calls canAccessOrg (lines 768-786), and the users lifecycle/purge paths call canAccessOrg before mutating (lines 378, 404). S
  - *Fix:* Add `if !s.canAccessOrg(r, p, tenantID) { writeError(403 FORBIDDEN); return }` at the top of the tenant update branch and in iamTenantLifecycle (both the purge and the status branches), mirroring productForAccess/iamUserLifecycle.
- **runtime TraceHandler bypasses the application layer and calls the repository directly** — [companion/runtime] `companion/internal/runtime/handler.go:18` · `bug` · effort `m`
  - *Qué:* TraceHandler declares an interface named traceUsecase whose own comment is "superficie del repo expuesta al handler" (the repo surface exposed to the handler), and the struct field is literally `repo`. wire/setup.go:773 wires it with the concrete repository: `runtime.NewTraceHandler(traceRepo)`. So the HTTP handler calls persistence (GetByID/ListByOrg/ListByTask) with no usecase/application layer in between — there is no place for run-trace authorization/business rules to live except inline in the handler (canAccessTraceOrg is done in the handler). The flat single `package runtime` (36 files,
  - *Fix:* Introduce a thin run-trace usecase (application service) between handler and repository, or at minimum rename the port to reflect it is a query service and keep org-scoping/authorization in that service rather than the HTTP handler. Longer term, split package runtime into handler/usecases/infra sub-packages so handler physically cannot import the repository type.
- **connector side-effect executes before persistence; SaveExecution failure loses the record but the external action already happened** — [companion] `companion/internal/connectors/usecases.go:301` · `bug` · effort `l`
  - *Qué:* In Usecases.Execute the real side effect runs at conn.Execute (line 301) and only afterwards is the result persisted via uc.repo.SaveExecution (line 317). These are two separate, non-atomic steps with no surrounding transaction. If SaveExecution fails with a non-conflict error (DB hiccup, serialization failure, connection drop), the function returns an error to the caller AFTER the external system (e.g. WhatsApp send, work-order notify) has already been mutated. The execution lock is released by the deferred ReleaseExecutionLock, so a retry with the same idempotency key will NOT find a prior r
  - *Fix:* Write an 'attempting' execution row (status=in_progress, unique on the idempotency tuple) BEFORE calling conn.Execute, then update it to success/failure after — so a crash/retry sees the prior attempt via GetExecutionByIdempotency and refuses to re-fire. Alternatively adopt an outbox: persist intent in a tx, execute the side effect from a worker that records terminal state. Companion already uses
- **watchers HTTP handler returns generic 500s that discard the error cause entirely (no log, no propagation)** — [companion] `companion/internal/watchers/handler.go:81` · `bug` · effort `s`
  - *Qué:* Every internal-error path in watchers/handler.go calls httpjson.WriteFlatError(w, http.StatusInternalServerError, "internal_error", "could not <verb> watcher") with a hardcoded message and throws away err (lines 81, 104, 131, 163, 185, 207, 220, 242, 272, 298, 308). Unlike httpjson.WriteFlatInternalError — which does slog.Error("internal error", ..., "error", err) (platform httpjson.go:68) — WriteFlatError does NOT log (httpjson.go:63). So when Create/List/Get/Update/Delete/RunWatcher fail, the real cause (DB error, marshal failure, capability-config parse error) is neither returned to the cli
  - *Fix:* Replace the 11 WriteFlatError(...StatusInternalServerError...) calls with httpjson.WriteFlatInternalError(w, err, "could not create watcher") so the platform helper logs the cause. This is a drop-in adopt of the platform package already imported in the file.
- **watcher RunWatcher: per-item proposal create + N updates + watcher update have no transaction; partial failures leave inconsistent proposal/Nexus state** — [companion] `companion/internal/watchers/usecases.go:510` · `bug` · effort `l`
  - *Qué:* processItem does a sequence of independent writes and remote calls with no atomic boundary: CreateProposal (line 510) -> nexus.SubmitRequest (line 544) -> executeAction/connector side effect (line 583) -> reportExecutionToNexus (line 594/598) -> UpdateProposal (line 611). Several failure windows leave durable inconsistency: (1) CreateProposal succeeds with nexus_request_id NULL, then SubmitRequest fails — the code marks the proposal failed (line 562-567) but if that UpdateProposal also fails it is only logged (line 566), leaving an orphan pending proposal with no nexus id that SyncPendingPropo
  - *Fix:* Make proposal lifecycle transitions idempotent and durable: persist the connector execution result keyed by the watcher idempotency key (watcher-execute-<proposalID>, set at usecases.go:652) and have the post-execution UpdateProposal failure retried or reconciled from the execution row rather than only logged. At minimum, treat the final UpdateProposal failure as a hard error that prevents countin
- **orgctx scopes by org_id only and ignores product_surface — nexus cannot partition data per product within an org** — [nexus] `nexus/internal/orgctx/orgctx.go:49` · `bug` · effort `l`
  - *Qué:* The canonical tenant in the Axis model is (org_id x product_surface) — see bff/tenancy.go:14-23 (axis_tenants with UNIQUE(org_id, product_surface)) and companion/internal/identityctx/context.go which carries ProductSurface end to end. nexus has NO equivalent: orgctx is the only request-scope helper and it preserves/returns ONLY X-Org-ID. Narrowed(r, principalOrg) (orgctx.go:49-54) resolves to an org string with no product dimension. Every consumer (orgctx.Requested/Narrowed) therefore collapses all of an org's products into one tenant. A principal authenticated for product A in org X sees, and
  - *Fix:* Adopt the companion pattern: introduce a nexus identityctx (or extend orgctx) that also extracts product_surface from the principal claims and the X-Product-Surface header, mirroring companion/internal/identityctx/context.go (fromPrincipal line 244, fromHTTPContext line 278, ProductSurface accessor line 148). Then thread (org_id, product_surface) as the scoping key through the org-scope helpers. L
- **auth middleware never extracts product_surface from JWT claims into the principal/context** — [nexus] `nexus/wire/auth.go:76` · `bug` · effort `m`
  - *Qué:* newAuthMiddleware (wire/auth.go:54-80) authenticates the principal and then calls orgctx.WithRequested with ONLY r.Header.Get(HeaderOrgID). product_surface is documented as an expected product-JWT claim (comment at wire/auth.go:129) and product JWTs are explicitly supported (newProductJWTAuthenticators), but the claim is dropped on the floor: it is never copied to the principal, the context, or any header. The OIDC mapper (newJWTAuthenticator, line 213) maps OrgID/Actor/Role/Scopes but not product_surface. Combined with identityhttp.Context having no product field (platform/authn/go/identityht
  - *Fix:* In the product-JWT and OIDC mappers, read product_surface from claims and propagate it: either set an X-Product-Surface header in the rebind (companion uses identityctx.HeaderProductSurface = 'X-Product-Surface', context.go:17) or store it in a nexus identity context. The cleanest fix is to add a ProductSurface field to platform/authn/go/identityhttp.Context and have WithPrincipal carry it, so all
- **requests table and List/GetByID queries have no product_surface column — product scoping is impossible in SQL** — [nexus] `nexus/internal/requests/repository.go:317` · `bug` · effort `l`
  - *Qué:* The requests table (migrations/0001_initial.up.sql:48-76, org_id added later in 0010) has no product_surface column. List (repository.go:317) filters only on status, action_type and org_id; selectRequestSQL (repository.go:485) and scanRequest (repository.go:498) carry org_id but no product. The Request domain entity (internal/requests/usecases/domain/entities.go:68-99) has OrgID *string and no ProductSurface. product_surface is required ONLY as a JSON key inside the action_binding JSONB blob (validateActionBinding, usecases.go:644; enforced by the JSON-schema in migrations/0022:351,361) — it i
  - *Fix:* Add a product_surface column to requests (and the other tenant-owned tables: approvals, policies, learning_proposals, findings, audit), backfill from action_binding.product_surface where present, persist it on insert from the resolved identity, and add it to the WHERE clause of List/scan plus the canAccessRequestOrg post-check. Index (org_id, product_surface, created_at DESC) to replace the org-on
- **approvals/ops/policies/learning/dashboard org-scope helpers all collapse product into org** — [nexus] `nexus/internal/approvals/authz.go:32` · `bug` · effort `l`
  - *Qué:* The org-only scoping is duplicated across every read/write surface, so the gap's blast radius is module-wide, not isolated to orgctx: approvals requestOrgScope (authz.go:32) and canAccessApprovalOrg (authz.go:49) compare only PrincipalOrgID vs approval.OrgID; ops opsOrgScope/writableOrg (internal/ops/handler.go:213,229) use orgctx.Narrowed(...) org-only; policies list (internal/policies/handler.go:130) sets filters.OrgID only; learning proposalOrgScope (internal/learning/handler.go:264) org-only; dashboard (internal/dashboard/handler.go:52) org-only; requests requestOrgScope/canAccessRequestOr
  - *Fix:* After introducing the product dimension (findings above), update each of these helpers to scope by (org_id, product_surface) and add product to the per-item access checks (canAccessApprovalOrg, canAccessRequestOrg). Consider centralizing the org+product scope resolution into one shared helper to stop the 6-way duplication of tenancy logic.
- **Handler classifies usecase errors via strings.Contains on err.Error() instead of typed domainerr** — [nexus/requests] `nexus/internal/requests/handler.go:101` · `debt` · effort `m`
  - *Qué:* The submit (L209-219) and simulate (L101-112) paths reverse-engineer HTTP status from substring matches: strings.Contains(errMsg, "unknown action_type"/"is disabled"/"not delegated") -> 403, and "missing required param"/"action_type schema"/"action_binding" -> 400. The attest path (L431) matches "status must be". These conditions originate in usecases.go as plain fmt.Errorf (L304 unknown action_type, L307 disabled, L322 not delegated, L880/883 schema validation, L1545/1547/1551 attestation field validation, L1562 attest status). This is exactly the message-coupling domainerr was built to remov
  - *Fix:* Return typed errors from the usecase: domainerr.NotFound/BusinessRule for unknown/disabled action_type, domainerr.Forbidden for not-delegated, domainerr.Validation for schema/binding/attestation-field failures, domainerr.BusinessRule for the attest-status precondition. Then the handler classifies via domainerr.IsX (already imported) and the strings.Contains branches disappear.
- **ops handler maps every usecase error to HTTP 500, hiding NotFound/Validation/Conflict** — [nexus/ops] `nexus/internal/ops/handler.go:80` · `bug` · effort `m`
  - *Qué:* Unlike requests/findings, the ops handler never inspects error kind. retryCallbackDelivery (L80-83) returns 500 for a non-existent delivery id (should be 404). createLegalHold (L116-118) and createExport (L166-168) return 500 for any usecase validation/conflict. listCallbackDeliveries/listLegalHolds/listExports/summary/runReconciliation all funnel into WriteFlatInternalError. Repositories DO produce domainerr (grep confirms domainerr in 10 repos incl. pgx.ErrNoRows->NotFound), so typed errors are available but discarded at this boundary — clients get 500 for client-correctable conditions.
  - *Fix:* Classify with domainerr.IsNotFound/IsConflict/IsValidation (or httperr.WriteFrom once flat-mode exists) before the WriteFlatInternalError fallback in each ops handler method.
- **findings handler maps ALL errors (incl. read-path DB failures) to 400 VALIDATION with raw internal error text** — [nexus/findings] `nexus/internal/findings/handler.go:105` · `bug` · effort `s`
  - *Qué:* createRule (L88), listRules (L105), listArchivedRules (L127), and listFindings (L317) do WriteFlatError(400, 'VALIDATION', err.Error()) for ANY error. listRules/listArchivedRules/listFindings are read paths that hit the DB: a DB outage returns '400 VALIDATION: <raw sql/pgx error>' — wrong status class (should be 500) AND leaks internal error text to the client. The same file defines writeUsecaseError (L391) that correctly does NotFound->404 / fallback->WriteFlatInternalError, but it is only wired into a couple of paths (e.g. L291), not the list/create paths.
  - *Fix:* Route all findings error paths through the existing writeUsecaseError helper (extend it to also classify Validation/Conflict), and never pass raw err.Error() to the client for non-validation errors.
- **axisFetch reads X-Tenant-ID from localStorage as a hidden side-channel** — [console] `console/src/api.ts:391` · `debt` · effort `m`
  - *Qué:* axisFetch takes orgId as an explicit argument (set as X-Axis-Org-ID) but pulls the active tenant from localStorage.getItem('axis.tenant_id') instead of a parameter. The X-Tenant-ID header — which the BFF treats as the source of truth for org+product+scopes — is therefore implicit global mutable state. Every caller (chat adapter, CRUD client, all 24 refresh fetches) depends on App having written the right value to localStorage first, with no compile-time link. Tests must mutate localStorage to exercise it, and any code path that fetches before refresh() writes the key sends a stale or empty ten
  - *Fix:* Thread the tenant id through axisFetch as an explicit parameter (e.g. axisFetch(path, { orgId, tenantId, ...init })) or capture it in the closure of axisCrudHttpClient/axisChatAdapter the same way orgId already is. Keep localStorage only as the persistence layer read once in App, not inside the fetch primitive.
- **Bulk and review actions swallow errors (try/finally with no catch)** — [console] `console/src/IAMControlCenter.tsx:132` · `bug` · effort `m`
  - *Qué:* applyBulkAction in IAMControlCenter (132-148) and both applyReviewAction (108-117) and applyBulkAction (119-132) in AgentsControlCenter wrap the network calls in try/finally with NO catch. The call sites invoke them with `void`, so a failed archive/trash/purge/approve becomes an unhandled rejection and the user sees nothing — the row just silently stays. loadTenantOptions (IAM:445) and loadOrgOptions (Agents:632) similarly catch-and-reset to [] with no message. PromptScreens does this correctly (sets crudError), so the gap is inconsistent within the same module.
  - *Fix:* Add a catch that surfaces err.message into an inline error state, mirroring PromptScreens' crudError / AgentsControlCenter's profilesError. Ideally extract one useBulkAction helper that owns busy+error+reload so all three CRUD screens report failures the same way.
- **Brittle setTimeout + querySelector + .click('Nuevo') hack to open the create form** — [console] `console/src/AgentsControlCenter.tsx:92` · `debt` · effort `m`
  - *Qué:* To trigger CrudPage's create form, three places (IAMControlCenter:96-105, AgentsControlCenter:92-100, AgentsControlCenter:294-302) schedule a 0ms setTimeout, querySelector into CrudPage's private DOM ('.crud-page-shell__header-actions > .actions-row > .actions-row > button'), find the button whose textContent === 'Nuevo', and synthesize a .click(). This reaches into the platform component's internal markup AND hard-codes the Spanish label, so it silently breaks on any CrudPage refactor or i18n change, and is the kind of effect-driven DOM poke that caused prior render hazards. It is also the so
  - *Fix:* Have platform-crud-ui expose an imperative create trigger — either a ref handle (useImperativeHandle: ref.current.openCreate()) or an onReady(helpers) callback, or accept a controlled createOpen prop. Then App/IAM/Agents call that instead of faking a click. Removes three copies of the hack and the DOM/i18n coupling.
- **resolveAppContext fails OPEN to org-only path with full principal scopes when X-Tenant-ID absent** — [bff] `bff/main.go:360` · `bug` · effort `m`
  - *Qué:* When X-Tenant-ID is present, resolveAppContext (main.go:344-359) correctly derives scopes from the user's TENANT role via scopesForTenant — Axis is the authz source of truth. But when the header is absent it falls through (main.go:360-369) to the legacy org-only branch and forwards p.Scopes verbatim — i.e. the broad scope set minted at authenticate-time from the Clerk axis_role/org_role claim (axisPrincipalFromIdentity in identity.go:116, which can return defaultAdminScopes()). So a caller who simply omits X-Tenant-ID bypasses the tenant-membership downgrade and proxies to companion/nexus with
  - *Fix:* Adopt tenant.StrictModeEnabled(): when strict and no X-Tenant-ID resolves, deny via domainerr.TenantMissing() instead of forwarding principal scopes. At minimum, never forward claim-derived p.Scopes downstream on the legacy path — re-derive from the Axis store like the tenant path does. Keep org x product resolution in axis.

### 🟠 MED (25)

- **Legacy org-member path (userRefOrg / axis_org_members) coexists with new tenant-member path behind a runtime TenantByID probe** — [bff] `bff/iam_simple_handlers.go:658` · `debt` · effort `l`
  - *Qué:* resolveUserRef (line 658) classifies a 'tenant__<id>__<user>' row id by probing TenantByID: a hit => userRefTenant (axis_tenant_members), a genuine errNotFound => userRefOrg (legacy axis_org_members via updateIAMMember/memberUserView). Create/update/lifecycle each branch on this (createIAMUserView lines 447-505, updateIAMUserView 523-560, iamUserLifecycle 411-430). The SAME row-id prefix 'tenant__' encodes BOTH a real tenant id and a legacy org id, disambiguated only by a live DB lookup. This is fragile dual-path debt: a tenant row that is transiently missing, or a legacy org id that happens t
  - *Fix:* Make the row id self-describing for legacy vs tenant (distinct prefixes, e.g. org__ vs tenant__) so classification is lexical, not a DB probe. Then schedule removal of the userRefOrg branch + memberUserView once members are migrated into axis_tenant_members.
- **resolveAppContext legacy org-only branch issues internal JWT with raw Clerk-claim scopes and no tenant** — [bff] `bff/main.go:361` · `debt` · effort `m`
  - *Qué:* resolveAppContext has two paths. With X-Tenant-ID it correctly derives scopes from scopesForTenant(tenant role, platform roles) — the Axis source of truth. Without it (lines 361-369) it falls back to selectedOrg + p.Scopes (the Clerk-claim-derived scopes) and TenantID:"", defaulting product_surface to 'axis-console'. So whether companion/nexus get Axis-derived or Clerk-derived authority depends purely on whether the browser sent X-Tenant-ID. The legacy branch forwards the claim scopes downstream verbatim, defeating the 'authz source of truth is Axis, not Clerk' intent stated in identity.go:160
  - *Fix:* Drive scopes from Axis (platform roles + membership) in BOTH branches; treat the no-tenant branch as a deprecated compatibility shim with an explicit deadline, or require X-Tenant-ID for all app-plane proxying once the console always sends it.
- **OIDC-without-identity-provider produces a principal the IAM/Control handlers can't authorize** — [bff] `bff/main.go:121` · `bug` · effort `m`
  - *Qué:* provider := firstNonEmpty(cfg.IdentityProvider, cfg.AuthMode) (main.go:121) only sets s.identity when provider=="clerk". So AuthMode="oidc" with AXIS_IDENTITY_PROVIDER unset is a valid, startable config (newServer succeeds): tokens are verified and a principal is built by the inline Map (main.go:133-154), but s.identity is nil, so PrincipalFromClaims/SyncPrincipal never run. That means axisPrincipalFromIdentity is skipped (scopes come raw from the token), the actor is never persisted to axis_users, and no platform role bridge runs -> Control Plane and any actor-membership-based authz silently
  - *Fix:* Either require an identity provider whenever AuthMode is oidc/clerk (fail fast in newServer if provider is unset), or run the actor-provisioning + role-derivation logic for any non-dev authenticated principal independent of whether s.identity is a clerk adapter. Add a startup-config validation test.
- **clerkWebhook unauthenticated-fallthrough branch runs only outside clerk mode and writes to the store unsigned** — [bff] `bff/iam_handlers.go:59` · `bug` · effort `s`
  - *Qué:* clerkWebhook: if ClerkWebhookSecret is empty AND AuthMode=="clerk" it 503s (iam_handlers.go:59-62). But if the secret is empty and AuthMode is anything else (dev/stg/preview/oidc), signature verification is skipped entirely and the body is parsed and written to the IAM store (CreateUser/CreateOrg at iam_handlers.go:80-105, or s.identity.HandleWebhook if identity happens to be set). So in dev/stg/preview an attacker can POST /api/webhooks/clerk with no signature and inject/overwrite axis_users and axis_orgs rows. The route is mounted unconditionally in routes() (main.go:175) for all modes. The
  - *Fix:* Reject unsigned webhooks in every mode when no secret is configured (return 503/401 regardless of AuthMode), or gate the whole /api/webhooks/clerk route registration on a configured secret. Add a test asserting an unsigned webhook is rejected in dev mode.
- **updateIAMUser local-fallback creates a membership the clerk path does not (silent role-write divergence)** — [bff] `bff/identity.go:74` · `bug` · effort `m`
  - *Qué:* updateIAMUser's local (s.identity==nil, dev) branch, when orgID!='' and input.Role!='', does UpdateMember and on errNotFound falls back to UpsertMember to CREATE the org membership (identity.go:74-82). The clerk branch (identity.go:67-69 -> clerkIdentityAdapter.UpdateUser) only PATCHes an existing membership when input.Role!='' and never creates a missing one (identity_clerk.go:302-306 guards on input.Role but calls UpdateMember which PATCHes /memberships/{user}; a non-member yields a Clerk 404 error, not a create). So 'edit role on a user who is not yet a member' succeeds in dev (creates memb
  - *Fix:* Make the clerk UpdateMember upsert-on-missing like UpsertMember (it already has memberExists), or make the dev path also refuse to create memberships on update, so both modes have identical semantics. Add a parity test for 'update role of a non-member'.
- **controlProvisionTenant creates tenant then owner member without a transaction** — [bff] `bff/control_handlers.go:96` · `bug` · effort `m`
  - *Qué:* controlProvisionTenant does CreateTenant (axis_tenants UPSERT, line 96) and then, when owner_user_id is present, UpsertTenantMember (axis_tenant_members, line 107) as two separate writes. If the second fails (transient error, bad user_id FK violation, crash between calls) the tenant exists with zero members and the caller gets a 500 — the orphaned tenant is then silently reused by the idempotent CreateTenant ON CONFLICT on a retry, but the owner may still never be attached if the retry path differs, or duplicate provisioning attempts proliferate ownerless tenants. The provisioning primitive sh
  - *Fix:* Execute CreateTenant + the owner UpsertTenantMember inside a single sql.Tx so a tenant is never created without its requested owner. Add a store method ProvisionTenant(ctx, tenant, ownerUserID) that opens one tx and does both inserts, returning the created tenant.
- **sqlIAMStore.CreateOrg inserts org then upserts owner member non-atomically** — [bff] `bff/iam.go:727` · `bug` · effort `m`
  - *Qué:* CreateOrg (iam.go:727-751) does the axis_orgs INSERT (line 733), then findUser, then UpsertMember to make the actor an owner in axis_org_members (line 746) — two writes, no tx. A failure after the org insert but before/at the member upsert returns an error to the caller while the org row already exists; the actor who created it is NOT an owner, so ListOrgsForActor (which JOINs axis_org_members WHERE status='active') will not return the org to its creator — they created an org they cannot see or administer. The memory store has the same shape (iam.go:243-246) but is atomic under its mutex; only
  - *Fix:* Wrap the axis_orgs INSERT and the owner axis_org_members UPSERT in one sql.Tx inside CreateOrg so an org is always created with its owner membership. The Clerk POST stays outside the tx (ordered first), reconciled by webhook; the two local rows must commit together.
- **clerkIdentityAdapter.CreateUser/UpdateUser: external Clerk call + multiple local writes with no rollback** — [bff] `bff/identity_clerk.go:261` · `bug` · effort `l`
  - *Qué:* CreateUser (lines 235-271) does Clerk POST /users, then store.CreateUser (axis_users), then UpsertMember (Clerk POST membership + axis_org_members). If store.CreateUser fails after the Clerk user is created, the Clerk identity is orphaned (no local row, no compensating Clerk DELETE) — and because axis_users.email is UNIQUE, a retry that re-creates the Clerk user can still fail locally on the duplicate, wedging the flow. If UpsertMember fails after store.CreateUser, the user exists locally with no org membership. UpdateUser (lines 273-308) has the same shape: Clerk PATCH, then store.UpdateUser,
  - *Fix:* Make the two LOCAL writes (store.CreateUser + UpsertMember; store.UpdateUser + UpdateMember) atomic via a single sql.Tx in a store method. For the Clerk side, add compensation: on local-tx failure after a successful Clerk POST /users, issue a best-effort Clerk DELETE /users/{id} (or document that the user.created webhook + unique-email find-or-create reconciles it). Order: Clerk write first, then
- **resolveAppContext discards PlatformRolesForUser error, silently downgrading platform admins to 403 on store failure** — [bff] `bff/main.go:350` · `bug` · effort `s`
  - *Qué:* resolveAppContext does `platformRoles, _ := s.iam.PlatformRolesForUser(...)` (line 350) and then branches on isPlatformAdmin(platformRoles). If the platform-roles lookup fails (DB error), the error is dropped and platformRoles is empty, so isPlatformAdmin returns false and a genuine platform admin is forced down the TenantMembership path and gets a 403 "user is not a member of the requested tenant" (line 356) - a real authz outage masquerades as a clean denial, with no log line. The same silent-discard pattern in session() (main.go:245-247) is only informational, but here it gates the downstre
  - *Fix:* Propagate the PlatformRolesForUser error: on err return a 5xx/INTERNAL from resolveAppContext (or at minimum log it) instead of treating a store failure as 'no platform role'. Fix the identical discard in controlAPI the same way.
- **Control plane handlers leak raw internal error strings in 500 responses** — [bff] `bff/control_handlers.go:47` · `bug` · effort `s`
  - *Qué:* Every controlAPI sub-handler returns the raw store error to the client: controlListOrgs (line 47), controlCreateOrg (line 64), controlListTenants (line 73), controlProvisionTenant (lines 103,108), controlGrantPlatformRole (line 142), controlAddTenantMember (line 164) all do `writeError(w, http.StatusInternalServerError, "INTERNAL", err.Error())`. This discloses SQL/driver internals (table names, constraint names, anything pgx puts in the error) to the API consumer, and maps clerkAPIError 4xx outcomes to a blanket 500. It is also inconsistent with the rest of the BFF which returns the static "r
  - *Fix:* Return a static client-facing message (e.g. "control plane request failed") and log err server-side; reuse writeStoreError so NOT_FOUND / identity-not-configured / clerk 4xx map correctly instead of always 500.
- **Lifecycle/update handlers swallow the post-mutation view read error and return 200 with an empty user** — [bff] `bff/iam_simple_handlers.go:420` · `bug` · effort `s`
  - *Qué:* After a successful tenant-member mutation, the handler rebuilds the response view ignoring failure: iamUserLifecycle does `view, _ = s.tenantUserView(...)` (line 420) and `view, _ = s.memberUserView(...)` (line 429). tenantUserView returns (IAMUserView{}, false) when ListTenantMembers errors or the row is missing (lines 626-637), but the bool is discarded, so the handler then calls writeStoreResult with err==nil and emits HTTP 200 plus a zero-valued user (empty id/email/role). The console sees a 'success' carrying a blank user. The write itself succeeded, but the response is silently corrupt a
  - *Fix:* Check the ok/err from tenantUserView/memberUserView; if the view can't be rebuilt, either re-read authoritatively and return its error, or respond 204/return the known fields rather than a zero struct with 200.
- **runtime handler serializes the persistence type StoredTrace directly to the HTTP wire** — [companion/runtime] `companion/internal/runtime/handler.go:59` · `debt` · effort `s`
  - *Qué:* getByID/list call httpjson.WriteJSON(w, ..., trace) where trace is StoredTrace, defined in runtime/repository.go:28 (embeds RunTrace plus OrgID/UserID/TaskID/Error with json tags). The persistence struct's json tags double as the public API contract, so any storage-shape change silently changes the HTTP response, and internal fields (e.g. OrgID/UserID) leak to clients. Every other companion domain (assist, connectors, watchers, tasks, memory entries) maps domain→DTO via dto.*ToResponse before writing; run-traces are the exception.
  - *Fix:* Add a TraceResponse DTO (e.g. in a runtime handler/dto package) and a mapping func; write that instead of StoredTrace. Drop the json tags from the repository struct once it is no longer the wire type.
- **package runtime is a flat 36-file package with no handler/usecases/domain boundaries** — [companion/runtime] `companion/internal/runtime/orchestrator.go:1` · `debt` · effort `l`
  - *Qué:* Unlike assist/connectors/memory/tasks/watchers (which split handler.go / usecases.go / usecases/domain/entities.go and a handler/dto sub-package), all of runtime is a single `package runtime`: HTTP adapters (handler.go, governance_handler.go, observability_handler.go import net/http), orchestration core (orchestrator.go), persistence (repository.go, governance_repository.go, observability_repository.go with StoredTrace and json tags), and provider adapters (provider.go, ponti_runtime.go) coexist in one import scope. The compiler cannot enforce that core logic does not reach transport or persis
  - *Fix:* Split runtime into runtime/handler (net/http adapters + dto), runtime/usecases (orchestrator/governance/observability services), runtime/usecases/domain (RunTrace and value types, no json tags), and runtime/infra (repositories, ponti/provider clients), mirroring the assist/tasks layout.
- **watcher LastResult / LastRunAt update failure is swallowed, so RunWatcher reports success while persistence silently failed** — [companion] `companion/internal/watchers/usecases.go:218` · `bug` · effort `m`
  - *Qué:* RunWatcher computes the result, then UpdateWatcher to persist LastRunAt/LastResult; the error is only slog.Error'd (line 218-220) and the function still returns the result as success. Combined with the handler returning 200, an operator sees a healthy run with up-to-date last_run_at in the API response object, but the DB row was never updated — the next scheduled run still sees the stale last_run_at, and dashboards reading the persisted row diverge from what the run actually reported. The marshal failure of the result (line 213) is likewise degraded to '{}' and continues.
  - *Fix:* Either propagate the UpdateWatcher error (return it so the job marks the run failed and retries) or, if best-effort is intended, surface it in the returned WatcherResult / job result payload so the sync/observability layer can detect the divergence instead of only a log line.
- **connector execution: post-side-effect Nexus result report failure is swallowed in task and watcher executors** — [companion] `companion/internal/tasks/usecases_execution.go:491` · `bug` · effort `m`
  - *Qué:* reportExecutionToNexus (tasks usecases_execution.go:468-498, and the watcher twin at watchers/usecases.go:849-868) fires after the connector side effect to tell Nexus the action ran. On error or status>=400 it only logs slog.Warn (line 492-497 / 862-867) and returns void. Nexus is the source of truth for approval/execution state (per the gating comment in connectors/usecases.go:223-231); if the report is lost, Nexus believes an approved action was never executed, which can drive re-approval / re-execution or a stuck request. There is no retry, no outbox, no persisted 'report pending' marker —
  - *Fix:* Persist a 'nexus_report_pending' marker (or enqueue a job via the existing jobs.Repository the watchers package already injects) when ReportResult fails, and reconcile it from a loop, instead of dropping it to a warn log. The task execution already records actions/artifacts transactionally-adjacent; attach the report outcome to that record so a sweeper can retry.
- **execution lock + idempotency-check + side-effect race: lock guarantees single in-flight but not single-execution across lock release** — [companion] `companion/internal/connectors/usecases.go:276` · `bug` · effort `m`
  - *Qué:* The lock window (lines 276-298) acquires companion_connector_execution_locks, re-checks idempotency, runs the side effect (line 301), saves (line 317), then the deferred ReleaseExecutionLock (line 286) deletes the lock. Because the lock is released as soon as Execute returns — including on the SaveExecution-failed path — a concurrent or subsequent caller that acquires the freed lock and finds no persisted row (save failed) will execute the side effect again. The lock only serializes concurrent attempts; it does nothing to prevent re-execution once released with no durable result. This is the s
  - *Fix:* Tie lock lifetime to a durable terminal record: do not DELETE the lock row on the failure path until a result row (success or terminal failure) exists, or fold the lock + result into one row whose presence both blocks concurrency and records the outcome. Simplest: insert the result row inside the same logical step as releasing the lock, in one transaction.
- **requireScope is duplicated byte-for-byte across all 13 internal/*/authz.go files** — [nexus/*] `nexus/internal/requests/authz.go:18` · `debt` · effort `m`
  - *Qué:* func requireScope(w,r,scopes...) bool { if HasNoAuthContext||HasAnyScope {return true}; WriteFlatError(403,FORBIDDEN,'missing required scope'); return false } is identical in actiontypes, approvals, audit, contracts, dashboard, delegations, evidence, findings, learning, ops, policies, rbac, requests authz.go. 13 copies; any change to the auth gate (e.g. message, status, audit logging) must be made in 13 places.
  - *Fix:* Hoist a single nexus-internal helper (e.g. internal/httpx.RequireScope) used by all packages, OR adopt platform/http/gin/go rbac/auth helpers if nexus migrates off net/http. Since nexus is net/http (not gin), the platform gin helpers don't fit directly — extract a net/http RequireScope into platform/authn/go/identityhttp (it already owns HasAnyScope/HasNoAuthContext) so all Axis net/http services
- **contracts/learning handlers leak raw error strings and fall back to string-matching for validation** — [nexus/contracts] `nexus/internal/contracts/handler.go:139` · `bug` · effort `m`
  - *Qué:* contracts Validate (L134-140) checks domainerr.IsNotFound then blanket-maps everything else to 400 VALIDATION with raw err.Error() — a DB/transport failure becomes a 400 leaking internals. learning IngestProposal (L191-204) checks domainerr.IsConflict then falls back to strings.Contains(msg, 'is required') for 400, else 500 — same fragile message coupling as requests. policies/handler.go L611/L615 and rbac/handler.go L248 also pass raw err.Error() into 400/409 bodies.
  - *Fix:* Make these usecases return domainerr.Validation/Conflict and classify by kind; pass a fixed client message (or domainerr.Message()) rather than raw err.Error() to avoid internal-detail leakage.
- **GetByName failure is conflated into 'unknown action_type', masking infra errors as client errors** — [nexus/requests] `nexus/internal/requests/usecases.go:303` · `bug` · effort `s`
  - *Qué:* In Submit (L302-304) and Simulate (~L955), any error from actionTypes.GetByName(...) — including a DB/transport failure — is collapsed into fmt.Errorf('unknown action_type: %s'). The handler then maps that substring to 403 FORBIDDEN. So a Postgres outage during action-type lookup surfaces to the caller as '403 unknown action_type', not a 5xx. Genuine not-found and infrastructure failure must be distinguished.
  - *Fix:* Distinguish in the usecase: if domainerr.IsNotFound(atErr) (or the checker returns a sentinel) return domainerr.NotFound/BusinessRule('unknown action_type'); otherwise wrap and propagate as internal so it maps to 5xx.
- **Org-scope tenancy helpers duplicated with divergent signatures across ~12 handlers** — [nexus/*] `nexus/internal/ops/handler.go:213` · `debt` · effort `l`
  - *Qué:* The same tenancy decision (cross_org -> allowAll or narrow to orgctx; else bound to X-Org-ID/principal; HasNoAuthContext -> allowAll; else fail-closed) is reimplemented as opsOrgScope/writableOrg (ops L213-242), requestOrgScope/canAccessRequestOrg/bindParamsToPrincipalOrg (requests L285-694), and writableOrg/canAccessOrg/resolveOrgID across contracts, findings, learning, delegations, approvals, audit, evidence, policies, rbac, actiontypes authz.go. Signatures differ ((*string,bool) vs (*string,bool,bool)), so the fail-closed semantics are easy to get subtly wrong per copy. The shared orgctx pa
  - *Fix:* Extract one nexus-internal OrgScope(r) (*string, allowAll bool, ok bool) used by all handlers, layered on orgctx. Strategically, migrate this org-based scoping to platform/security/go/tenant (tenant.ID + ctx + strict-mode) so the cross_org/fail-closed logic is the agnostic platform piece rather than 12 bespoke copies.
- **onRefreshShell prop is dead; parent shell never refreshes after IAM mutations** — [console] `console/src/IAMControlCenter.tsx:42` · `bug` · effort `s`
  - *Qué:* IAMControlCenterProps declares onRefreshShell (line 42) and App passes onRefreshShell={refresh} (App.tsx:515), but the prop is never called anywhere inside IAMControlCenter. After creating/archiving/purging an org (tenant) or user via the IAM CRUDs, the App shell's session.tenants, workspace selectors and orgOptions are never re-fetched, so the global Org/Producto dropdowns go stale until a manual refresh. This is a real behavioral gap plus a dead prop.
  - *Fix:* Call props.onRefreshShell() after successful create/bulk mutations (e.g. in applyBulkAction's success path and in the CrudPage onChanged equivalent), or remove the prop if the staleness is acceptable. Don't leave it wired-but-unused.
- **selectionColumn duplicated verbatim across three CRUD files** — [console] `console/src/IAMControlCenter.tsx:323` · `debt` · effort `s`
  - *Qué:* The identical generic selectionColumn<T> (checkbox cell with stopPropagation + aria-label) is copy-pasted in IAMControlCenter.tsx:323, AgentsControlCenter.tsx:536, and PromptScreens.tsx:547 — byte-for-byte the same. Any change to selection UX (e.g. select-all, indeterminate, styling) must be edited in three places.
  - *Fix:* Extract one selectionColumn into a shared console module (e.g. console/src/lib/crud.tsx) and import it in all three. It is generic and platform-agnostic enough to be a candidate to upstream into @devpablocristo/platform-crud-ui as a built-in bulk-selection column.
- **CreateAndBulkActions / ProfileCreateAndBulkActions / PromptBulkActions are near-identical copies** — [console] `console/src/AgentsControlCenter.tsx:554` · `debt` · effort `m`
  - *Qué:* AgentsControlCenter defines CreateAndBulkActions (554) and ProfileCreateAndBulkActions (589) which are character-for-character identical, plus IAMControlCenter.CreateAndBulkActions (370) and PromptScreens.PromptBulkActions (565) differ only in which lifecycle buttons appear. All render the same Nuevo + Archivar/Papelera/Restaurar/Eliminar/Limpiar + selected-count layout with the same disabled logic. Four implementations of one toolbar.
  - *Fix:* Extract a single BulkActionsBar component (props: createLabel, view, selectedCount, busy, supportsTrash, onCreate/onClear/onBulkAction) into the shared console crud module. The two Agents copies can be collapsed immediately. Strong candidate to live in platform-crud-ui alongside the lifecycle toolbar since the archive/trash/restore/purge vocabulary is the platform lifecycle contract.
- **set-state-in-effect: org/product mirroring effects fight the controlled selectors** — [console] `console/src/AgentsControlCenter.tsx:85` · `debt` · effort `m`
  - *Qué:* ESLint react-hooks/set-state-in-effect fires 16 times. The pattern is effects that mirror a prop into local state: AgentsControlCenter setSelectedOrgId(orgId) on [orgId] (85) and setSelectedIds([]) on [selectedOrgId] (89); IAMControlCenter setSelectedUserOrgId(props.orgId) (78); App's auto-select effects (149,158,178,183,190). selectedOrgId in Agents is a pure mirror of the orgId prop with no independent updater, so it is redundant state synced via an effect (cascading render). Given this module already had an infinite-loop/flicker incident, these mirror effects are the live hazard surface.
  - *Fix:* For pure prop mirrors (Agents selectedOrgId, IAM selectedUserOrgId) drop the state and use the prop/derived value directly, or initialize from prop and remove the effect. For selection-clearing on view/org change, clear inside the same handler that changes view/org rather than via an effect. App's first-match selectors can stay but should be the only writers.
- **App refresh() fans out 24 parallel fetches on every org/product change with no batching or scoping** — [console] `console/src/App.tsx:91` · `debt` · effort `l`
  - *Qué:* refresh() issues 24 load() calls in one Promise.all and is the [refresh] dep of the mount effect (138) plus is re-run in full after every runAction mutation (132). Because refresh is rebuilt on any change to [externalTenantId, orgId, productSurface], switching the global Org or Producto selector re-fires all 24 requests, including ones unrelated to the change (session, health, nexus approvals/policies/action-types/delegations don't depend on productSurface). Earlier this exact coupling produced ERR_INSUFFICIENT_RESOURCES (documented at lines 81-89). It works now but is fragile and over-fetches
  - *Fix:* Split into independent useEffects keyed by their real deps (session/health once; nexus on orgId; companion data on orgId+productSurface) so a product switch only refetches product-scoped data. After a mutation, refetch only the affected slice instead of all 24. Consider the platform CrudDataSource list pattern for the list-shaped endpoints so refetch is owned per resource.

### 🟡 LOW (19)

- **session endpoint returns both axis_role/org_role (Clerk) and tenants/platform_roles (Axis) — dual model leaked to the console** — [bff] `bff/main.go:248` · `debt` · effort `s`
- **ensureActorUser only runs on tenant create and partially duplicates SyncPrincipal identity upsert** — [bff] `bff/iam_handlers.go:194` · `debt` · effort `s`
- **createIAMUser / updateIAMUser memory+sql fallback: CreateUser then UpsertMember non-atomic** — [bff] `bff/identity.go:52` · `bug` · effort `s`
- **iamTenants POST: ensureActorUser write result discarded before createIAMOrg** — [bff] `bff/iam_simple_handlers.go:89` · `debt` · effort `s`
- **session() discards store errors and renders a store outage as an empty (access-revoked-looking) session** — [bff] `bff/main.go:245` · `debt` · effort `s`
- **memory review/summary/audit responses skip the DTO boundary the module otherwise enforces** — [companion/memory] `companion/internal/memory/handler.go:349` · `debt` · effort `s`
- **domain entities carry json tags, coupling the pure domain core to its wire representation** — [companion/{assist,tasks,memory,connectors,watchers}] `companion/internal/memory/usecases/domain/agent_memory.go:15` · `debt` · effort `m`
- **connectors registry adapter reaches into another domain's HTTP client (watchers/pymesclient)** — [companion/connectors] `companion/internal/connectors/registry/pymes.go:13` · `debt` · effort `m`
- **identityctx mixes a pure identity value type with HTTP request extraction in one package** — [companion/identityctx] `companion/internal/identityctx/context.go:39` · `debt` · effort `m`
- **mustPayloadHash is named like a panicking helper but silently swallows errors** — [companion/connectors] `companion/internal/connectors/usecases.go:663` · `debt` · effort `s`
- **connector Execute logs Nexus authorization error then re-wraps and returns it as a generic 500-class error** — [companion] `companion/internal/connectors/usecases.go:256` · `debt` · effort `s`
- **watchers handler emits non-canonical snake_case error codes despite using domainerr internally** — [companion] `internal/watchers/handler.go:121` · `debt` · effort `s`
- **Mixed json.NewDecoder(r.Body) vs httpjson.DecodeJSON — strict decoding applied inconsistently** — [companion] `internal/agentfleet/handler.go:82` · `debt` · effort `s`
- **Error code string is 'VALIDATION' but the canonical domainerr/httperr code is 'VALIDATION_ERROR'** — [nexus/*] `nexus/internal/requests/handler.go:70` · `debt` · effort `s`
- **axis.tenant_id written twice (in refresh and in a dedicated effect)** — [console] `console/src/App.tsx:164` · `debt` · effort `s`
- **Lifecycle toolbar-actions builder duplicated (iamLifecycleToolbarActions vs lifecycleToolbarActions)** — [console] `console/src/AgentsControlCenter.tsx:624` · `debt` · effort `s`
- **lifecycleBucket, formatStatus, stringValue and splitList/stringList duplicated across files** — [console] `console/src/AgentsControlCenter.tsx:655` · `debt` · effort `s`
- **Three identical `PlatformCrudPage as unknown as` generic re-casts** — [console] `console/src/AgentsControlCenter.tsx:29` · `debt` · effort `s`
- **nexus_assist error strings start with capitalized 'Gemini' (ST1005)** — [companion] `companion/internal/nexus_assist/proposer.go:184` · `debt` · effort `s`

## 2. ♻️ Reuse → `platform` (23)

Código de Axis que reimplementa lo que `platform` ya provee (ADOPT) o agnóstico a extraer (EXTRACT).

| Sev | Módulo | Dónde | Hallazgo | Acción (`reuse`) |
|---|---|---|---|---|
| med | bff | `bff/tenancy.go:27` | Hand-rolled tenant Membership + role normalization duplicates platform kernels/saas tenant domain | Adopt kernels/saas/go/tenant (Membership domain type + NormalizeRole/NewMembership) for tenant-member modeling and recon |
| med | bff | `bff/main.go:494` | writeJSON/writeError duplicate httpjson.WriteJSON/WriteError byte-for-byte | github.com/devpablocristo/platform/http/go/httpjson (adopt WriteJSON/WriteError) |
| med | bff | `bff/iam_handlers.go:234` | writeStoreError reimplements domainerr->HTTP mapping (errNotFound instead of domainerr.NotFound) | github.com/devpablocristo/platform/errors/go/domainerr + http/go/httperr (adopt domainerr.NotFound/NotFoundf + httperr.WriteFrom) |
| med | bff | `bff/main.go:609` | hasScope/requireScope duplicate authz.HasScope/HasAnyScope | github.com/devpablocristo/platform/authz/go (adopt HasAnyScope; wrap for requireScope) |
| med | bff | `bff/main.go:463` | signInternalJWT is the issuer-side twin of authn/go/internaljwt (HS256 mint vs verify, duplicated claim shape) | extract:internaljwt.Signer (issuer-side mint in platform/authn/go/internaljwt to pair with its existing verifier) |
| med | bff | `bff/iam_handlers.go:109` | Clerk webhook svix verification duplicates kernels/saas/go/clerkwebhook | github.com/devpablocristo/platform/kernels/saas/go/clerkwebhook (adopt verifySvix / Handler) |
| med | companion/assist | `companion/internal/assist/usecases.go:327` | assist application layer depends on the sibling runtime flat package for LLM types instead of the platform kernel | platform/kernels/ai/go (coreai) — already a direct go.mod dependency of companion (v0.2.0). It provides Provider/ChatRequest/Message; that IS the LLM port. |
| med | companion | `internal/identityctx/context.go:82` | authz/go scope helpers reimplemented in three places | github.com/devpablocristo/platform/authz/go (HasScope / HasAnyScope) |
| med | companion | `internal/ops/handler.go:121` | http/go/pagination not adopted: limit parsing duplicated ~14 times | github.com/devpablocristo/platform/http/go/pagination (ParseParams / NormalizeLimit / BuildResult) |
| med | nexus/requests | `nexus/internal/requests/handler.go:95` | requests/handler.go reimplements error->HTTP mapping that platform httperr.Normalize/WriteFrom already provides | Adopt platform/http/go/httperr: replace each manual IsX ladder + WriteFlatInternalError fallback with httperr.WriteFrom(w, err) once usecases return typed domainerr. NOTE blocker: httperr writes the ENVELOPE shape {error:{code,message}} via httpjson.WriteError, whereas nexus standardized on the FLAT shape {code,message} via WriteFlatError (114 call sites). Adopting httperr as-is changes the wire contract; the agnostic fix is to add a WriteFlatFrom/flat-mode to platform httperr (extract the Normalize+flat-write), then nexus reuses Normalize and keeps its flat envelope. |
| med | console | `console/src/App.tsx:51` | Hand-rolled localStorage access instead of platform-browser storage namespace | @devpablocristo/platform-browser/storage (createBrowserStorageNamespace) |
| med | bff | `bff/main.go:609` | hasScope/requireScope reimplement authz.HasScope / HasAnyScope | github.com/devpablocristo/platform/authz/go |
| med | bff | `bff/main.go:494` | writeJSON/writeError/writeStoreError reimplement httpjson + httperr + domainerr mapping | github.com/devpablocristo/platform/http/go/httperr (and httpjson, errors/go/domainerr) |
| med | bff | `bff/main.go:343` | Tenant-resolution errors use ad-hoc errors.New instead of domainerr Tenant* factories | github.com/devpablocristo/platform/errors/go/domainerr (TenantMissing/TenantMismatch/TenantNotFound) |
| med | bff | `bff/main.go:629` | Clerk claim extraction (firstClaim/claimScopes/audience/issuer) duplicates identity ExternalIdentity + ClaimsResolver | github.com/devpablocristo/platform/kernels/saas/go/identity (ExternalIdentityFromClaims + adapters/clerk.DefaultMapping) and the issuer/audience matching in identity.ClaimsResolver |
| med | bff | `bff/main.go:463` | Internal-JWT HS256 sign/verify hand-rolled on both sides of the bff<->companion boundary | extract: platform/authn/go/internaljwt (service-to-service HS256 minter/verifier) |
| low | bff | `bff/iam.go:1049` | No cursor pagination on list endpoints despite platform/http/go/pagination; audit/users lists silently capped | github.com/devpablocristo/platform/http/go/pagination (adopt ParseParams/Result/BuildResult) |
| low | bff | `bff/iam.go:1170` | slugify duplicates kernels/saas/go/tenant.NormalizeSlug | github.com/devpablocristo/platform/kernels/saas/go/tenant.NormalizeSlug (adopt) or extract:validate/go/stringutil.Slugify |
| low | bff | `bff/identity.go:236` | normalizeRole/NormalizeRole and Membership reimplement saas tenant role normalization | github.com/devpablocristo/platform/kernels/saas/go/tenant.NormalizeRole (adopt) or extract: a shared role-normalization helper |
| low | bff | `bff/main.go:430` | securityHeaders middleware duplicates http/go/httpserver.SecurityMiddleware (and is weaker) | github.com/devpablocristo/platform/http/go/httpserver.SecurityMiddleware (adopt) |
| low | bff | `bff/iam.go:118` | IAM store (orgs/users/members/invitations) is a parallel reimplementation of kernels/saas identity+users+org | github.com/devpablocristo/platform/kernels/saas/go/{identity,users,org} (adopt repositories/usecases) — partial |
| low | nexus/* | `nexus/internal/requests/handler.go:250` | Per-handler limit parsing duplicates platform pagination; cursor pagination unused | Adopt platform/http/go/pagination.ParseParams+NormalizeLimit for limit handling (per-package Config for the differing defaults/ceilings), and pagination.Result[T]/BuildResult for list envelopes to get consistent cursor pagination instead of unbounded LIMIT scans. |
| low | bff | `bff/main.go:587` | splitScopes defined three times across the codebase (bff + platform identity) | extract: platform/security/go/scopes (or export the existing identity.splitScopes/parseScopes as a public helper) |

## 3. 🧹 Dead code (14)

- `console/src/IAMControlCenter.tsx:66` — Dead state: axisTenants / activeAxisTenants fetched but never rendered · *Delete axisTenants, activeAxisTenants, loadTenantOptions, the effect at 88-90, and the listIAMTenants import. Removes a wasted fetch and the unused-var lint err*
- `companion/internal/tasks/handler.go:741` — SA4010: chat handler builds msgs slice that is never used · *remove — delete the msgs make+loop (handler.go:741-744). ChatResponseFromRuntimeResult already maps result.Messages internally. Confirm with the author that msg*
- `bff/identity.go:26` — Invitation provider methods (Create/Update/ListInvitations) are implemented for both modes but unreachable by any route · *Either wire an /api/iam invitations route (and test it in dev) or delete the invitation methods from the interface/adapter/store to remove the untested divergen*
- `bff/iam.go:139` — ListAuditEvents store method has no route; audit is write-only and the read path is untested in both modes · *Add a read route (e.g. GET /api/iam/audit gated by axis:orgs:admin) with a dev-mode test, or remove ListAuditEvents until needed.*
- `companion/internal/connectors/usecases.go:527` — SeedDefaultConnectors named to persist connectors but only logs — dead/misleading side-effect surface · *Either delete the function and its caller, or rename/redocument it to reflect it only emits capability templates (the inline comment at 534-536 already explains*
- `console/src/lib/tenant.ts:26` — Exported preferred() helper is unused (App inlines the same logic) · *Use preferred() in the two auto-select effects (setOrgId(preferred(workspaceOrgs,'cristo.tech')) etc.), or delete preferred + its test if the inline form is pre*
- `console/src/PromptScreens.tsx:14` — Unused useRef import and stale eslint-disable directive · *Drop useRef from the PromptScreens import. Re-evaluate the App.tsx:123 disable: either move it to actually cover the 125 useCallback dep warning or remove it; r*
- `bff/identity.go:109` — bff deleteIAMMember wrapper is unused (no route, no test) · *remove — delete the (*server).deleteIAMMember method (identity.go:109-114). The DeleteMember port and its implementations stay (still tested/used elsewhere). If*
- `companion/internal/watchers/usecases_test.go:126` — watchers test: fakePymes fake and all its methods are unused (U1000) · *remove — delete the fakePymes type and its 7 methods from usecases_test.go (lines ~126-162). If a pymes port test is intended later, it can be re-added; right n*
- `companion/internal/capabilities/manifest.go:139` — capabilities.FromConnectorCapabilities has no callers anywhere · *remove — delete FromConnectorCapabilities (manifest.go:139-151). If the connector-capability path is dead, also check whether FromConnectorCapability (singular)*
- `companion/internal/capabilities/manifest.go:153` — capabilities.LoadFS / LookupOperation flagged dead but are test-only (false positive for from-main) · *keep (for now) — they compile-depend on tests, so removing them requires removing those test cases too. Recommend a product decision: if connector-operation loo*
- `nexus/internal/callbacks/http_publisher.go:44` — nexus callbacks.HTTPApprovalPublisher / .Publish are test-only; prod uses OutboxApprovalPublisher · *keep (decision needed). It is intentional-looking: a simpler direct-HTTP publisher kept alongside the outbox one. If the team has settled on the outbox publishe*
- `nexus/internal/requests/attestation_verifier.go:61` — nexus requests.SignAttestationForTest is a test helper (correctly test-only) · *keep — it is a test fixture for the attestation verifier. Minor improvement: it currently lives in a non-test file (attestation_verifier.go), so it ships in the*
- `nexus/internal/requests/usecases.go:191` — nexus requests.WithPendingApprovalCreator Option is never called · *remove — delete WithPendingApprovalCreator (usecases.go:191-193). The auto-wiring at line 211 covers the only real path. Keep the interface and field. If a futu*

## 4. Apéndice — descartados (19) (by-design / falso-positivo)

- [bff] RemoveTenantMember is implemented in both stores + interface but never called — tenant-member removal relies on an FK cascade that the memory store cannot honor — The finding's two load-bearing claims are factually wrong; only a minor dead-code observation survives.  1. "memory store has no FK, so a purge there leaves orp
- [bff] SyncPrincipal writes Clerk org membership + axis_role into the legacy org model on every login — The finding's factual claims about the code are accurate, but its core premise and proposed fix are not currently actionable, so I am downgrading rather than co
- [bff] resolveAppContext / canAccessOrg reimplement principal-from-context + tenant membership gating that platform middleware already provides — The finding's central premise is false and the principal-plumbing fix would cause a regression. (1) FALSE: it claims platform middleware "already provides" tena
- [bff] Actor identity is created eagerly every request in clerk mode but only lazily on tenant POST in dev mode — The finding's mechanical observations are accurate but it overstates the consequence and proposes a fix that does not fix the stated symptom. Verified facts: (1
- [bff] session endpoint returns empty axis_role/org_role/auth_method semantics in dev mode vs populated in clerk mode — The descriptive code facts are accurate: in dev/preview/stg mode (bff/main.go:194-201) the Principal's Claims is nil, so session() (main.go:255-256) emits axis_
- [bff] ensureActorUser error discarded before tenant-scoped user creation — The finding's concrete harm cannot actually occur, and its stated trigger is factually wrong.  WRONG TRIGGER: ensureActorUser (iam_handlers.go:194-208) calls s.
- [bff] agent-profiles and prompts proxies enforce scope but never bind the actor to the resolved org — The factual description is accurate (agent_profiles_handlers.go:27/30 and prompts_handlers.go:29/32 call requireScope + selectedOrg but not canAccessOrg, unlike
- [bff] Tenant resolution never uses canonical security/go/tenant ctx keys (raw headers + ctx untouched) — False positive built on a wrong architectural premise. The BFF talks to companion/nexus across an HTTP process boundary; tenant context is propagated via a sign
- [bff] lifecycleBucket/statusForIAMAction reimplement lifecycle.LifecycleState + actions (with a 'trash' vs 'trashed' divergence) — The finding's central thesis is inverted and the cited location is wrong. Code facts: statusForIAMAction/lifecycleBucket live in iam_simple_handlers.go (lines 8
- [companion] watchers create/list/update treat domain validation errors as 500 instead of 400 — The finding's core justifications are factually wrong. (1) Usecases.Create (usecases.go:109-118) and Update (131-146) never call resolveWatcherCapabilityConfig,
- [companion] http/go/httperr not adopted: domain-error→HTTP status hand-mapped in every handler — The fix as written is unsafe and the headline is factually wrong for the named file. Evidence: (1) WIRE-CONTRACT MISMATCH the finding ignores: companion uses ht
- [companion] assist handler flattens domainerr kind to 400, losing platform error semantics — FALSE POSITIVE. The finding's load-bearing claim is factually wrong about code routing. It asserts that domainerr.Conflict from UpsertPack (usecases.go:104, "as
- [companion] agentprofiles defines raw errors.New sentinels instead of domainerr, blocking platform normalization — The literal code facts check out: companion/internal/agentprofiles/domain.go:12-14 declares ErrNotFound/ErrValidation/ErrConflict via errors.New, handler.go:193
- [nexus] Driven SQL adapter lives in the same package as handler+usecases (port and adapter not separated) — The factual observations are accurate: nexus/internal/requests/repository.go is `package requests` and imports jackc/pgx/v5 + sharedpostgres (lines 14-15, 19),
- [console] axisCrudHttpClient reinvents an HTTP client that just wraps axisFetch — The finding's core premise is false. Verified against the actual platform source at axis/console/node_modules/@devpablocristo/platform-crud-ui/src/.  WHAT'S COR
- [console] Console LoadState/load reimplements async-state that platform CrudDataSource covers for list screens — Largely false-positive / by-design. Verified at console/src/App.tsx and console/src/lib/load.ts.  TRUE parts: load.ts (lines 1-26) does hand-roll LoadState<T> +
- [bff] bff does not import platform/security/go/tenant despite being the tenancy authority — The finding's premise facts are largely true (bff/go.mod:5 requires only platform/authn/go; bff resolves org×product in resolveAppContext at main.go:343 and min
- [companion] jobs.MemoryRepository is test-only — deadcode-from-main false positive — Verified false positive. jobs.MemoryRepository (companion/internal/jobs/memory_repository.go) is an in-memory test double, not dead code. The only non-test refe
- [companion] productevals LoadPacks/EvaluatePack/FindRepoFile are test+script only — false positive — Verified: the finding is a correctly-identified FALSE POSITIVE, so there is no real actionable defect. EvaluatePack, LoadPacks, and FindRepoFile in companion/in
