name: Deploy Console DEV

on:
  push:
    branches: [develop]
    paths:
      - "console/**"
      - "console.nginx.conf.template"
      - "Dockerfile.console"
      - ".github/workflows/deploy-console-dev.yml"
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch or ref to deploy"
        required: false
        default: "develop"

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-console-dev
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID_DEV }}
      GCP_REGION: ${{ vars.GCP_REGION }}
      WIF_PROVIDER: ${{ vars.WIF_PROVIDER_DEV }}
      WIF_SERVICE_ACCOUNT: ${{ vars.WIF_SERVICE_ACCOUNT_DEV }}
      ARTIFACT_REGISTRY: ${{ vars.ARTIFACT_REGISTRY }}
      CLOUD_RUN_SERVICE_ACCOUNT: ${{ vars.AXIS_CONSOLE_CLOUD_RUN_SERVICE_ACCOUNT_DEV || vars.CLOUD_RUN_SERVICE_ACCOUNT_DEV }}
      IMAGE_NAME: ${{ vars.AXIS_CONSOLE_IMAGE_NAME_DEV || 'axis-console' }}
      SERVICE_NAME: ${{ vars.AXIS_CONSOLE_CLOUD_RUN_SERVICE_NAME_DEV || 'axis-console' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.ref || github.ref_name }}

      - name: Validate workflow inputs
        env:
          AXIS_BFF_BASE_URL: ${{ vars.AXIS_BFF_BASE_URL_DEV }}
        run: |
          missing=0
          for var_name in \
            GCP_PROJECT_ID \
            GCP_REGION \
            WIF_PROVIDER \
            WIF_SERVICE_ACCOUNT \
            ARTIFACT_REGISTRY \
            CLOUD_RUN_SERVICE_ACCOUNT \
            AXIS_BFF_BASE_URL; do
            if [[ -z "${!var_name}" ]]; then
              echo "::error::$var_name is required"
              missing=1
            fi
          done
          exit "$missing"

      - name: Auth GCP
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Configure Docker auth
        run: gcloud auth configure-docker "${GCP_REGION}-docker.pkg.dev" --quiet

      - name: Build and push image
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          image_uri="${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${ARTIFACT_REGISTRY}/${IMAGE_NAME}:${IMAGE_TAG}"
          docker build -f Dockerfile.console -t "$image_uri" .
          docker push "$image_uri"
          echo "IMAGE_URI=$image_uri" >> "$GITHUB_ENV"

      - name: Deploy to Cloud Run
        env:
          AXIS_BFF_BASE_URL: ${{ vars.AXIS_BFF_BASE_URL_DEV }}
        run: |
          gcloud run deploy "$SERVICE_NAME" \
            --project="$GCP_PROJECT_ID" \
            --region="$GCP_REGION" \
            --image="$IMAGE_URI" \
            --service-account="$CLOUD_RUN_SERVICE_ACCOUNT" \
            --set-env-vars="^|^AXIS_BFF_BASE_URL=${AXIS_BFF_BASE_URL}" \
            --allow-unauthenticated \
            --ingress=all \
            --port=80 \
            --min-instances=0 \
            --max-instances=3

      - name: Resolve service URL
        run: |
          service_url="$(gcloud run services describe "$SERVICE_NAME" \
            --project="$GCP_PROJECT_ID" \
            --region="$GCP_REGION" \
            --format='value(status.url)')"
          if [[ -z "$service_url" ]]; then
            echo "::error::Could not resolve service URL"
            exit 1
          fi
          echo "SERVICE_URL=$service_url" >> "$GITHUB_ENV"
          echo "Service URL: $service_url"

      - name: Smoke check
        run: |
          for i in {1..20}; do
            read -r latest_ready latest_created < <(gcloud run services describe "$SERVICE_NAME" \
              --project="$GCP_PROJECT_ID" \
              --region="$GCP_REGION" \
              --format='value(status.latestReadyRevisionName,status.latestCreatedRevisionName)')
            if [[ -n "$latest_created" && "$latest_ready" == "$latest_created" ]]; then
              curl -fsS "$SERVICE_URL/readyz"
              exit 0
            fi
            echo "attempt $i: latestReady=$latest_ready latestCreated=$latest_created, retrying in 3s..."
            sleep 3
          done
          echo "::error::Cloud Run did not mark the latest revision ready"
          exit 1
